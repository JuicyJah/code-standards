{
  "name": "standards-build",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Build tooling that parses the standards markdown into a catalog and validates SIDs.",
  "scripts": {
    "build": "node build-catalog.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "unified": "^11.0.5",
    "remark-parse": "^11.0.0",
    "vitest": "^2.1.8"
  }
}
